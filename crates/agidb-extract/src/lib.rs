//! Layer 2 — extraction. GLiNER ONNX entity/relation extraction.
//!
//! Phase 3 lands the real pipeline (`Extraction`, alias resolution, predicate
//! canonicalization, time-anchor parsing). Phase 2 uses a regex placeholder
//! that this crate re-exports until then.
