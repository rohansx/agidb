//! agidb CLI — open, observe, recall, consolidate, export, import.
//!
//! This is a stub. Real subcommands land alongside the corresponding phases.

fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt::init();
    eprintln!("agidb CLI — pre-alpha. See docs/phases/ for the build plan.");
    Ok(())
}
